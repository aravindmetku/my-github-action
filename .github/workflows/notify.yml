name: notification

on:
  workflow_run:
    workflows:
      - test
    types:
      - completed

jobs:
  preflight-job:
    name: Preflight Step
    runs-on: ubuntu-latest
    steps:
      - run: env
